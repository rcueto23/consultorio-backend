# ===========================================
# CONFIGURACIÓN DEL BACKEND - CONSULTORIO PSICOLÓGICO
# ===========================================
# IMPORTANTE: Copia este archivo como `.env` y completa con tus valores reales
# NO compartas el archivo .env con nadie, contiene información sensible

# ===========================================
# DATABASE - SUPABASE POSTGRESQL
# ===========================================
# Connection pooling para la aplicación (puerto 6543 con pgbouncer)
DATABASE_URL="postgresql://postgres.[PROJECT-REF]:[PASSWORD]@aws-0-us-east-1.pooler.supabase.com:6543/postgres?pgbouncer=true"

# Session mode para migraciones de Prisma (puerto 5432 sin pgbouncer)
DIRECT_URL="postgresql://postgres.[PROJECT-REF]:[PASSWORD]@aws-0-us-east-1.pooler.supabase.com:5432/postgres"

# ===========================================
# JWT - AUTENTICACIÓN
# ===========================================
# Clave secreta para firmar tokens JWT (usa una clave fuerte en producción)
JWT_SECRET=change_this_to_a_strong_random_secret_key

# Tiempo de expiración del token (7d = 7 días, 1h = 1 hora, 30m = 30 minutos)
JWT_EXPIRATION=7d

# ===========================================
# APLICACIÓN
# ===========================================
# Puerto donde se ejecutará el servidor
PORT=3001

# Entorno de ejecución (development, production, test)
NODE_ENV=development
